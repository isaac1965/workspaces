//let age = prompt("Ingresa tu edad: ");

//let value = age > 18 ? true: false;
//alert(value);

// otro ejemplo manera (2) de usar operadores ternarios
let num = prompt("Ingresa un numero menor que 5: ");
let resultado = (num < 3) ?  'hello':
(num < 4) ? 'como estas':
(num < 6) ? 'que haces':
'Que numero tan peculiar';
alert(resultado);
